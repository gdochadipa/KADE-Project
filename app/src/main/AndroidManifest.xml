<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.ocha.eplmatch">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Football App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".NavActivity"
            android:label="Football App">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailMatch" />
        <activity
            android:name=".MatchActivity"
            android:label="Football App"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DetailClub.DetailClubActivity"
            android:label="Football App"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DetailClub.DetailPlayer.DetailPlayerActivity" />
        <activity android:name=".Searching.SearchingMatchActivity" >
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
        </activity>
        <activity android:name=".Searching.SearchingTeamActivity">

        </activity>
    </application>

</manifest>